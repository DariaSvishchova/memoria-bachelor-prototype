# Memoria Backend

1. `cd backend`
2. `copy .env.example .env`
3. In `.env` den Wert `JWT_SECRET` durch eine lange zufällige Zeichenfolge ersetzen.
4. `npm install`
5. `npm start`

Daten: `backend/data/db.json`  
Uploads: `backend/uploads/`
